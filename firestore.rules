rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser clients receive no direct Firestore access. Public content,
    // orientation requests, audit records, and CMS operations are handled by
    // server routes using the Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
